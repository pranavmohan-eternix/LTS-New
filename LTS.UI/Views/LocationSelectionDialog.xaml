<Window x:Class="LTS.UI.Views.LocationSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Location"
        Width="360"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">

            <GroupBox Header="Chamber"
                      Margin="0,0,0,10">

                <ItemsControl ItemsSource="{Binding Chambers}"
                              Margin="8">

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <RadioButton Content="{Binding Identifier}"
                                         Tag="{Binding Identifier}"
                                         GroupName="LocationGroup"
                                         FontSize="14"
                                         Margin="0,4"
                                         Checked="RadioButton_Checked"
                                         Loaded="RadioButton_Loaded"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>

            </GroupBox>

            <GroupBox Header="Load Port">

                <ItemsControl ItemsSource="{Binding LoadPorts}"
                              Margin="8">

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <RadioButton Content="{Binding Identifier}"
                                         Tag="{Binding Identifier}"
                                         GroupName="LocationGroup"
                                         FontSize="14"
                                         Margin="0,4"
                                         Checked="RadioButton_Checked"
                                         Loaded="RadioButton_Loaded"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>

            </GroupBox>

        </StackPanel>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">

            <Button Content="Cancel"
                    Width="90"
                    Height="34"
                    Margin="0,0,10,0"
                    Click="BtnCancel_Click"/>

            <Button Content="Send"
                    Width="90"
                    Height="34"
                    Background="#2563EB"
                    Foreground="White"
                    Click="BtnSend_Click"/>

        </StackPanel>

    </Grid>

</Window>